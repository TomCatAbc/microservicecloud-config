spring:
  profiles:
  active:
  - dev

spring:
  profiles: dev
  application: 
    name: microservicecloud-config-atguigu-dev

spring:
  profiles: test
  application:
    name: microservcecloud-config-atguigu-test
